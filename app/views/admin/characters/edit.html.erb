Modifying character <b><%= @character.name %></b> for user: <b><%= @user.firstname %> <%= @user.lastname %></b><br>
<% provide(:url, admin_user_character_path(@character)) %>
<% provide(:backurl, admin_user_character_path(@character)) %>
<%= render 'admin/characters/characterform' %>
