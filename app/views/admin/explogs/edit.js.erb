$('.modal-title').html("Edit Exp")
$('.modal-body').html("<%= escape_javascript( render partial: 'admin/modals/explog', locals: {explog: @explog, pathurl: admin_user_explog_path(user_id: @user.id)} ) %>")
$(document).ready(function(){
  $('#expModal').modal('show');
});