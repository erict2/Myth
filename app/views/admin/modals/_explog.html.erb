<div class="modal-body">
  <%= bootstrap_form_with(model: @explog, url: pathurl) do |f| %>
    <%= f.select :name, [
      'Adjustment', 'Donation', 'Event', 'Feedback Letter', 'Level Up', 'Profession Purchase', 
      'Referral', 'Season Pass', 'Skill Refund',  'Go Big Bonus', 'Setup/Take Down', 'Volunteer' 
      ], label: 'Type' %>
    <%= f.text_field :description %>
    <%= f.text_field :acquiredate, label: 'Date: (YYYY-MM-DD)'%>
    <%= f.text_field :amount %>
    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
    <button type="submit" class="btn btn-primary">Submit</button>
  <% end %>
</div>