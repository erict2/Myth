<% provide(:url, admin_user_path(@user)) %>
<% provide(:backurl, admin_user_path(@user)) %>
<%= bootstrap_form_with(model: @user, url: yield(:url), local: true) do |form| %>

    <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:
      </h2>
        <ul>
          <% @user.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
    </div>
    <% end %>
    
    <%= form.text_field :firstname, label: 'First Name'%>
    <%= form.text_field :lastname, label: 'Last Name' %>
    <%= form.text_field :email %>
    <%= form.select :usertype, [ 'Admin', 'Player','Cast' ], label: 'User Type' %>
    </p><p>
        <%= form.submit 'Save' %>
        <%= link_to 'Cancel', yield(:backurl) %>
    </p>
    <% end %>